server:
  port: 8082
logging:
  level:
    root: INFO
    org.springframework: DEBUG
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://oauth2provider.local/.well-known/jwks